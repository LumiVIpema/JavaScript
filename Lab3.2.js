
//Lumi Valkyrie Ipema
//Software Developer MBO4

document.write ("Multiplier application"); //The title of the application.

var number1 = 3; //The set variable this can easily be changed to be another number the calculation will then be done with the new number.
var number2 = prompt ("3 x ?"); //this is the prompt you will get as a pop up.
var calculation = number1 * number2; //this piece makes sure your set data gets multiplied by the data given from the oudside source.
document.write ("<br>"+ number1+"x"+number2+"="+calculation); // the calculation will be shown on the page.